# Interfaces

- No shared schema or API changes in this task.
- Existing `AERIAL_*` config keys are unchanged.
- Added pure Lua helper functions in `lua/aerial_clock.lua`:
  - `ass_escape(text)`
  - `font_tag(font)`
  - `build_layer_ass_line(opts)`
